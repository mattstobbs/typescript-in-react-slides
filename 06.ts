export {};

function mapToGoodBoi(list: string[]) {
  return list.map(() => 'doggo');
}

function seriousFunction() {
  const fancyWords = [
    'extravagant',
    'flamboyant',
    'parlance',
    'despondent',
    'effervescence',
  ];

  return mapToGoodBoi(fancyWords);
}
